import { useRouter } from 'expo-router';
import React from 'react';
import { View } from 'react-native';

const Location = () => {
  const router = useRouter();

  return <View>Mine masina detailidesse</View>;
};

export default Location;
