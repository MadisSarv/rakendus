import { useRouter } from 'expo-router';
import React from 'react';
import { View } from 'react-native';
import { Text } from 'react-native-paper';

const Location = () => {
  const router = useRouter();

  return (
    <View>
      <Text>Mine masina detailidesse</Text>
    </View>
  );
};

export default Location;
